{
  "name": "@getpaseo/sync",
  "version": "0.1.104",
  "description": "Paseo encrypted UI state sync worker",
  "files": [
    "dist",
    "!dist/**/*.map"
  ],
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "node": "./dist/index.js",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "clean": "node ../../scripts/clean-package-dist.mjs",
    "build": "tsc -p tsconfig.json --incremental false",
    "build:clean": "npm run clean && npm run build",
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
